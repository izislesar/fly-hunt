# DATA_FETCH — fly-brain pin + FlyWire v783 cache

- Repo URL: https://github.com/erojasoficial-byte/fly-brain.git
- Clone path: /home/izislesar/Projects/fly_oxota/fly-brain (subdirectory; workdir root stays clean)
- Pinned SHA: 27cec28d (full: 27cec28d5d202eb004683fb4c1a1033eec8deea0)
- `git -C fly-brain rev-parse HEAD` == 27cec28d5d202eb004683fb4c1a1033eec8deea0 ✅ EXACT MATCH (HEAD is branch tip; no detached checkout needed)
- Fetch date (UTC): 2026-09-16T09:11:29Z
- Clone note: plain `git clone` stalled on LFS smudge (>120s); retried with `GIT_LFS_SKIP_SMUDGE=1 git clone` (OK), then `git lfs pull` (exit 0). `git lfs install` run before clone.

## Required source files (in fly-brain/)

| File | Exists | Size (bytes) |
|---|---|---|
| two_flies.py | YES | 46854 |
| fly_embodied.py | YES | 47661 |
| brain_body_bridge.py | YES | 34396 |
| procedural_arena.py | YES | 19290 |

All four present; vendor copy is READ-ONLY (do not edit).

## Data files (copied to /home/izislesar/Projects/fly_oxota/data/)

| File | Size | SHA256 |
|---|---|---|
| data/2025_Completeness_783.csv | 3327347 bytes (3.2M) | bbb847a4cc2caaa7a16349722d220c087317b946d148d4d592d94d250617a311 |
| data/2025_Connectivity_783.parquet | 100804642 bytes (97M ≈ 96MB expected) | efeb23fb99098e9c390f6869969b2a121a2ee92c833cfc45ecb2c1d8e1af0347 |

- `ls -lh data/2025_Connectivity_783.parquet` → 97M ✅ (~96MB)
- `sha256sum -c data/MANIFEST.sha256` → both OK ✅ (MANIFEST generated by real `sha256sum data/*`)
- `file` → Apache Parquet file (PAR1 magic), NOT an LFS pointer ✅

## LFS status

- `.gitattributes` tracks only `*.pt` and `*.mp4`; the v783 csv/parquet are regular git blobs (no LFS pull needed for them).
- `git lfs ls-files` after pull: `data/plastic_weights.pt`, `data/plastic_weights_fly0.pt`, `data/plastic_weights_fly1.pt`, `demo.mp4` — all resolved (`*`, real sizes: plastic_weights.pt 58M, demo.mp4 52M). No pointer files remain.

## Offline posture (for all later tasks)

- Network was allowed ONLY for this fetch task. All subsequent tasks MUST work offline.
- Data is cached at `/home/izislesar/Projects/fly_oxota/data/` and source at `/home/izislesar/Projects/fly_oxota/fly-brain/`.
- Fail-closed rule: any later step requiring network MUST fail and log to `out/fail_fetch.log` instead of silently proceeding (`--offline` semantics).

## Prepared commit message (DO NOT COMMIT — data/*.parquet must stay uncommitted)

```
chore(data): pin fly-brain 27cec28d v783
```
